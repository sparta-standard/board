spring:
  application:
    name: board

  datasource:
    url: jdbc:postgresql://localhost:5001/board
    username: jiyun
    password: 1205
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        highlight_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
